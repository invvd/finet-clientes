/**
 * Datos corporativos estáticos para documentos generados (ej. comprobantes de
 * pago, CU-52). El modelo `empresa` de Prisma solo tiene `nombre` y
 * `rut_empresa` — el resto se completa acá, mismo patrón que
 * `apps/view/app/_lib/company.ts` en el frontend.
 */
export const EMPRESA_DIRECCION = 'La Pintana, Región Metropolitana, Chile';
